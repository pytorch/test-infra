Important considerations when creating grafana dashboards:

* use Clickhouse mcp tools to research the schema of the data source
* always test the query using Clickhouse mcp before creating the dashboard