name: Build M1 Wheels

on:
  workflow_call:
    inputs:
      repository:
        description: 'Repository to checkout, defaults to ""'
        default: ""
        type: string
      ref:
        description: 'Reference to checkout, defaults to "nightly"'
        default: "nightly"
        type: string
      _setup_action:
        description: "Checkout action to use (MAINLY USED FOR TESTING PURPOSES ON test-infra)"
        default: "pytorch/test-infra/.github/actions/setup-binary-build@master"
        type: string

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        # TODO: Split this out into its own action to generate this
        # TODO: Find a way to limit matrix for PR testing
        python_version: ["3.8", "3.9", "3.10"]
    env:
      PYTHON_VERSION: ${{ matrix.python_version }}
      PACKAGE_TYPE: wheel
      REPOSITORY: ${{ inputs.repository }}
      REF: ${{ inputs.ref }}
    runs-on: macos-m1-12
    # If a build is taking longer than 60 minutes on these runners we need
    # to have a conversation
    timeout-minutes: 60
    steps:
      - uses: ${{ inputs._setup_action }}
        with:
          repository: ${{ inputs.repository }}
          ref: ${{ inputs.ref }}
          setup-miniconda: false
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install delocate-wheel
        run: |
          ${CONDA_RUN} python3 -m pip install delocate
      - name: Install torch dependency
        run: |
          source "${BUILD_ENV_FILE}"
          # shellcheck disable=SC2086
          ${CONDA_RUN} ${PIP_INSTALL_TORCH}
      - name: Build the wheel (bdist_wheel)
        run: |
          source "${BUILD_ENV_FILE}"
          ${CONDA_RUN} python3 setup.py bdist_wheel
      - name: Delocate wheel
        run: |
          ${CONDA_RUN} DYLD_FALLBACK_LIBRARY_PATH="${CONDA_ENV}/lib" delocate-wheel -v --ignore-missing-dependencies dist/*.whl
      - name: Upload wheel to GitHub
        uses: actions/upload-artifact@v3
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: dist/
      # TODO: Figure out upload method to s3

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}-${{ inputs.repository }}-${{ github.event_name == 'workflow_dispatch' }}
  cancel-in-progress: true
