name: Build and Test

on:
  push:
    branches:
      - main
    paths:
      - '.github/actions/checkout/**'
  pull_request:
    branches:
      - main
    paths:
      - '.github/actions/checkout/**'


# Note that when you see patterns like "ref: test-data/v2/basic" within this workflow,
# these refer to "test-data" branches on this actions/checkout repo.
# (For example, test-data/v2/basic -> https://github.com/actions/checkout/tree/test-data/v2/basic)

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./.github/actions/checkout
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: 24.x
      - uses: actions/checkout@v6
      - run: npm ci
      - run: npm run build
      - run: npm run format-check
      - run: npm run lint
      - run: npm test
      - name: Verify no unstaged changes
        run: __test__/verify-no-unstaged-changes.sh

  test:
    strategy:
      matrix:
        runs-on: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.runs-on }}
    defaults:
      run:
        working-directory: ./.github/actions/checkout

    steps:
      # Clone this repo
      - name: Checkout
        uses: actions/checkout@v6

      - name: Activate longpath support
        if: matrix.runs-on == 'windows-latest'
        run: git config --system core.longpaths true

      # Basic checkout
      - name: Checkout basic
        uses: ./.github/actions/checkout/
        with:
          repository: actions/checkout
          ref: test-data/v2/basic
          path: .github/actions/checkout/basic
      - name: Verify basic
        shell: bash
        run: __test__/verify-basic.sh

      # Clean
      - name: Modify work tree
        shell: bash
        run: __test__/modify-work-tree.sh
      - name: Checkout clean
        uses: ./.github/actions/checkout/
        with:
          repository: actions/checkout
          ref: test-data/v2/basic
          path: .github/actions/checkout/basic
      - name: Verify clean
        shell: bash
        run: __test__/verify-clean.sh

      # Side by side
      - name: Checkout side by side 1
        uses: ./.github/actions/checkout/
        with:
          repository: actions/checkout
          ref: test-data/v2/side-by-side-1
          path: .github/actions/checkout/side-by-side-1
      - name: Checkout side by side 2
        uses: ./.github/actions/checkout/
        with:
          repository: actions/checkout
          ref: test-data/v2/side-by-side-2
          path: .github/actions/checkout/side-by-side-2
      - name: Verify side by side
        shell: bash
        run: __test__/verify-side-by-side.sh

      # Filter
      - name: Fetch filter
        uses: ./.github/actions/checkout/
        with:
          filter: 'blob:none'
          path: .github/actions/checkout/fetch-filter

      - name: Verify fetch filter
        run: __test__/verify-fetch-filter.sh

      # Additional fetch refs
      - name: Additional fetch refs
        uses: ./.github/actions/checkout/
        with:
          path: .github/actions/checkout/additional-fetch-refs
          additional-fetch-refs: |
            refs/heads/main

      # Sparse checkout
      - name: Sparse checkout
        uses: ./.github/actions/checkout/
        with:
          sparse-checkout: |
            .github/actions/checkout/__test__
            .github/actions/checkout/dist
          path: .github/actions/checkout/sparse-checkout

      - name: Verify sparse checkout
        run: __test__/verify-sparse-checkout.sh

      # Disabled sparse checkout in existing checkout
      - name: Disabled sparse checkout
        uses: ./.github/actions/checkout/
        with:
          path: .github/actions/checkout/sparse-checkout

      - name: Verify disabled sparse checkout
        shell: bash
        run: set -x && ls -l sparse-checkout/.github/actions/checkout/src/git-command-manager.ts

      # Sparse checkout (non-cone mode)
      - name: Sparse checkout (non-cone mode)
        uses: ./.github/actions/checkout/
        with:
          sparse-checkout: |
            /.github/actions/checkout/__test__/
            /.github/actions/checkout/dist/
          sparse-checkout-cone-mode: false
          path: .github/actions/checkout/sparse-checkout-non-cone-mode

      - name: Verify sparse checkout (non-cone mode)
        run: __test__/verify-sparse-checkout-non-cone-mode.sh

      # LFS
      - name: Checkout LFS
        uses: ./.github/actions/checkout/
        with:
          repository: actions/checkout # hardcoded, otherwise doesn't work from a fork
          ref: test-data/v2/lfs
          path: .github/actions/checkout/lfs
          lfs: true
      - name: Verify LFS
        shell: bash
        run: __test__/verify-lfs.sh

      # Submodules false
      - name: Checkout submodules false
        uses: ./.github/actions/checkout/
        with:
          repository: actions/checkout
          ref: test-data/v2/submodule-ssh-url
          path: .github/actions/checkout/submodules-false
      - name: Verify submodules false
        run: __test__/verify-submodules-false.sh

      # Submodules one level
      - name: Checkout submodules true
        uses: ./.github/actions/checkout/
        with:
          repository: actions/checkout
          ref: test-data/v2/submodule-ssh-url
          path: .github/actions/checkout/submodules-true
          submodules: true
      - name: Verify submodules true
        run: __test__/verify-submodules-true.sh

      # Submodules recursive
      - name: Checkout submodules recursive
        uses: ./.github/actions/checkout/
        with:
          repository: actions/checkout
          ref: test-data/v2/submodule-ssh-url
          path: .github/actions/checkout/submodules-recursive
          submodules: recursive
      - name: Verify submodules recursive
        run: __test__/verify-submodules-recursive.sh

      # Worktree credentials
      - name: Checkout for worktree test
        uses: ./.github/actions/checkout/
        with:
          path: .github/actions/checkout/worktree-test
      - name: Verify worktree credentials
        shell: bash
        run: __test__/verify-worktree.sh worktree-test worktree-branch

      # Worktree credentials in container step
      - name: Verify worktree credentials in container step
        if: runner.os == 'Linux'
        uses: docker://bitnamisecure/git:latest
        with:
          args: bash .github/actions/checkout/__test__/verify-worktree.sh .github/actions/checkout/worktree-test container-worktree-branch

      # Basic checkout using REST API
      - name: Remove basic
        if: runner.os != 'windows'
        run: rm -rf basic
      - name: Remove basic (Windows)
        if: runner.os == 'windows'
        shell: cmd
        run: rmdir /s /q basic
      - name: Override git version
        if: runner.os != 'windows'
        run: __test__/override-git-version.sh
      - name: Override git version (Windows)
        if: runner.os == 'windows'
        run: __test__\\override-git-version.cmd
      - name: Checkout basic using REST API
        uses: ./.github/actions/checkout/
        with:
          repository: actions/checkout
          ref: test-data/v2/basic
          path: .github/actions/checkout/basic
      - name: Verify basic
        run: __test__/verify-basic.sh --archive

  test-proxy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./.github/actions/checkout
    container:
      image: ghcr.io/actions/test-ubuntu-git:main.20240221.114913.703z
      options: --dns 127.0.0.1
    services:
      squid-proxy:
        image: ubuntu/squid:latest
        ports:
          - 3128:3128
    env:
      https_proxy: http://squid-proxy:3128
    steps:
      # Clone this repo
      - name: Checkout
        uses: actions/checkout@v6

      # Basic checkout using git
      - name: Checkout basic
        uses: ./.github/actions/checkout/
        with:
          repository: actions/checkout
          ref: test-data/v2/basic
          path: .github/actions/checkout/basic
      - name: Verify basic
        run: __test__/verify-basic.sh

      # Basic checkout using REST API
      - name: Remove basic
        run: rm -rf basic
      - name: Override git version
        run: __test__/override-git-version.sh
      - name: Basic checkout using REST API
        uses: ./.github/actions/checkout/
        with:
          repository: actions/checkout
          ref: test-data/v2/basic
          path: .github/actions/checkout/basic
      - name: Verify basic
        run: __test__/verify-basic.sh --archive

  test-bypass-proxy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./.github/actions/checkout
    env:
      https_proxy: http://no-such-proxy:3128
      no_proxy: api.github.com,github.com
    steps:
      # Clone this repo
      - name: Checkout
        uses: actions/checkout@v6

      # Basic checkout using git
      - name: Checkout basic
        uses: ./.github/actions/checkout/
        with:
          repository: actions/checkout
          ref: test-data/v2/basic
          path: .github/actions/checkout/basic
      - name: Verify basic
        run: __test__/verify-basic.sh
      - name: Remove basic
        run: rm -rf basic

      # Basic checkout using REST API
      - name: Override git version
        run: __test__/override-git-version.sh
      - name: Checkout basic using REST API
        uses: ./.github/actions/checkout/
        with:
          repository: actions/checkout
          ref: test-data/v2/basic
          path: .github/actions/checkout/basic
      - name: Verify basic
        run: __test__/verify-basic.sh --archive

  test-git-container:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./.github/actions/checkout/basic
    container: bitnamisecure/git:latest
    steps:
      # Clone this repo
      - name: Checkout
        uses: actions/checkout@v6
        with:
          path: .github/actions/checkout/localClone

      # Basic checkout using git
      - name: Checkout basic
        uses: ./.github/actions/checkout/localClone/.github/actions/checkout/
        with:
          repository: actions/checkout
          ref: test-data/v2/basic
          path: .github/actions/checkout/basic
      - name: Verify basic
        run: |
          if [ ! -f "./basic-file.txt" ]; then
              echo "Expected basic file does not exist"
              exit 1
          fi

          # Verify .git folder
          if [ ! -d "./.git" ]; then
            echo "Expected ./.git folder to exist"
            exit 1
          fi

          # Verify auth token
          git config --global --add safe.directory "*"
          git fetch --no-tags --depth=1 origin +refs/heads/main:refs/remotes/origin/main

      # needed to make checkout post cleanup succeed
      - name: Fix Checkout v6
        uses: actions/checkout@v6
        with:
          path: .github/actions/checkout/localClone

  test-output:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./.github/actions/checkout
    steps:
      # Clone this repo
      - name: Checkout
        uses: actions/checkout@v6
        with:
          path: .github/actions/checkout/actions-checkout

      # Basic checkout using git
      - name: Checkout basic
        id: checkout
        uses: ./.github/actions/checkout/actions-checkout/.github/actions/checkout/
        with:
          repository: actions/checkout
          path: .github/actions/checkout/cloned-using-local-action
          ref: test-data/v2/basic

      # Verify output
      - name: Verify output
        run: |
          echo "Commit: ${{ steps.checkout.outputs.commit }}"
          echo "Ref: ${{ steps.checkout.outputs.ref }}"

          if [ "${{ steps.checkout.outputs.ref }}" != "test-data/v2/basic" ]; then
            echo "Expected ref to be test-data/v2/basic"
            exit 1
          fi

          if [ "${{ steps.checkout.outputs.commit }}" != "82f71901cf8c021332310dcc8cdba84c4193ff5d" ]; then
            echo "Expected commit to be 82f71901cf8c021332310dcc8cdba84c4193ff5d"
            exit 1
          fi
