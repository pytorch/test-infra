name: Claude Code

on:
  issue_comment:
    types: [created]
  issues:
    types: [opened]

jobs:
  claude-code:
    uses: ./.github/workflows/_claude-code.yml
    permissions:
      contents: read
      pull-requests: write
      issues: write
      id-token: write
    secrets: inherit
