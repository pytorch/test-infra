# Source shared environment directly (failsafe)
if [ -f ~/.shell_env ]; then
    source ~/.shell_env
fi

# Source .bashrc for additional bash-specific settings
if [ -f ~/.bashrc ]; then
    source ~/.bashrc
fi

# Show MOTD on login
if [ -f /etc/motd ]; then
    cat /etc/motd
fi