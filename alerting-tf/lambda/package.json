{
  "name": "alerting-lambda",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.js --sourcemap",
    "lint": "tsc --noEmit",
    "format": "prettier -w .",
    "format:check": "prettier -c ."
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.137",
    "esbuild": "^0.21.5",
    "prettier": "^3.3.3",
    "typescript": "^5.4.5"
  }
}
