# Local-only secrets for Terraform (not committed)
# Copy to: secrets.local.tfvars (same folder) and fill values.
# Git ignores *.tfvars*, so the real file will stay local.

# Shared token for Grafana webhook authentication (X-Grafana-Token)
webhook_grafana_token = "replace-with-random-long-secret"
