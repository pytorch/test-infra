git_url("https://github.com/pytorch/ios-certificates")

storage_mode("git")

type("appstore")

app_identifier(["com.pytorch.torchci"])

# Optionally override via environment variables:
#   MATCH_GIT_URL - git URL for certificates repo
#   MATCH_PASSWORD - passphrase for decrypting the repo

# For more information about Match, see:
#     https://docs.fastlane.tools/actions/match/
