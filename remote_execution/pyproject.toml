[tool.ruff]
line-length = 120

[tool.ruff.lint]
# Enable common lint rules
select = ["E", "F", "W", "I", "UP", "SIM", "RUF"]
ignore = ["N999"]  # Allow module names with hyphens

[tool.flake8]
max-line-length = 120
extend-ignore = ["E203", "W503"]
