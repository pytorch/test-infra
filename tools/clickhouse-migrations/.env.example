# CLICKHOUSE_HOST accepts a bare host or a full URL (https:// and :8443 are
# stripped). CLICKHOUSE_ENDPOINT is an accepted alias (the repo's CI wires the
# host in under that name); CLICKHOUSE_HOST takes precedence when both are set.
CLICKHOUSE_HOST=
CLICKHOUSE_PORT=8443
CLICKHOUSE_USERNAME=
CLICKHOUSE_PASSWORD=
