[project]
name = "torchci"
dynamic = ["version"]

[build-system]
requires = ["setuptools==68.2.2", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-dir]
"torchci" = "../torchci"
