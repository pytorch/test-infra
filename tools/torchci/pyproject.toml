[project]
name = "torchci"
dynamic = ["version"]

[build-system]
requires = ["setuptools==70.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-dir]
"torchci" = "../torchci"
